#include <iostream>
#include <cmath>
using namespace std;

typedef unsigned int ent;

ent expo(ent a, ent p, ent m) {
  if (p==0) return (a mod 
}

bool suspect(ent b, ent n) {
  ent t,u;
  u = n-1;
  t = 0;
  while (u%2==0) {
    ++t;
    u = u/2;
  }
}

int main() {
  return 0;
}
